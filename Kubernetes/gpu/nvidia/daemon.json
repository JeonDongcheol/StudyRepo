{
    "_comment" : "NVIDIA Docker 2.0 설치 및 설정을 위한 daemon.json 으로, GPU가 있는 Node sudo vi /etc/docker/daemon.json을 수정",
    "default-runtime": "nvidia",
    "runtimes": {
        "nvidia": {
            "path": "/usr/bin/nvidia-container-runtime",
            "runtimeArgs": []
        }
    }
}
